# ===============================
# 🚀 DOCKER NGINX WEB PAGE PROJECT
# ===============================

# 📁 STEP 1 — CREATE FOLDER
# Create a new folder anywhere on your system:
mkdir mydockerproject
cd mydockerproject

# ===============================
# 📄 index.html
# ===============================
# Create a file named index.html with the following code:

cat > index.html << 'EOF'
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to My Docker Project</title>
  <style>
    body {
      background-color: #f4f4f9;
      font-family: Arial, sans-serif;
      text-align: center;
      padding-top: 100px;
    }
    h1 {
      color: #0078D7;
      font-size: 3em;
    }
    p {
      color: #333;
      font-size: 1.2em;
    }
  </style>
</head>
<body>
  <h1>Hello from Docker + Nginx!</h1>
  <p>This webpage is served using a Docker container 🚀</p>
</body>
</html>
EOF

# ===============================
# 🐳 Dockerfile
# ===============================
# Create a file named Dockerfile with this code:

cat > Dockerfile << 'EOF'
# Use the official Nginx image
FROM nginx:latest

# Copy local HTML file into Nginx default directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80
EOF

# ===============================
# ⚙️ DOCKER COMMANDS
# ===============================

# Step 1: Verify Docker installation
docker --version

# Step 2: Run a test container (to check Docker works)
docker run hello-world

# Step 3: Build the Docker image (make sure you’re inside the mydockerproject folder)
docker build -t mydockerproject:v1 .

# Step 4: Run the Docker container
docker run -d -p 8080:80 mydockerproject:v1

# Step 5: View running containers (optional)
docker ps

# Step 6: Open your browser and visit:
# 👉 http://localhost:8080

# Step 7: To stop the container (optional)
# docker stop <container_id>

# ===============================
# ✅ PROJECT SUMMARY
# ===============================
# 1️⃣ Folder Name: mydockerproject
# 2️⃣ Files: index.html, Dockerfile
# 3️⃣ Commands build the image and run it
# 4️⃣ Opens your custom HTML page on http://localhost:8080
